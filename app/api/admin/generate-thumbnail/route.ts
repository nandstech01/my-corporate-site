import { NextRequest, NextResponse } from 'next/server';
import OpenAI from 'openai';

const openai = new OpenAI({
  apiKey: process.env.OPENAI_API_KEY
});

export async function POST(request: NextRequest) {
  try {
    const body = await request.json();
    const { scriptTitle, scriptHook, scriptBody, scriptCta, relatedNewsTitle } = body;

    if (!scriptTitle) {
      return NextResponse.json(
        { success: false, error: 'タイトルは必須です' },
        { status: 400 }
      );
    }

    console.log('🎨 サムネイル文言生成開始');
    console.log(`  台本タイトル: ${scriptTitle}`);
    console.log(`  Hook: ${scriptHook?.substring(0, 50)}...`);
    console.log(`  関連ニュース: ${relatedNewsTitle || 'なし'}`);

    // GPT-5でサムネイル文言を生成
    const systemPrompt = `あなたはYouTubeショート動画（縦型）のサムネイル文言生成のエキスパートです。

🎯【目的】瞬間的なクリックと感情を誘発する

🔥【最重要】バズるサムネイル文言の3大要素:
1. **衝撃の対立構造**を明確に示す
2. **常識の敗北**を強調する  
3. **数字・強烈な言い切り**でインパクトを出す

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【タイトル構造】3-8文字以内（厳守）

✅ 対立構造型（最強）:
   - 「常識vs AI」「数vs体験」「速さvs精度」
   - 「駆除vs環境」「伝統vs拡散」
   - 視覚: 画面を2分割（左=常識、右=AI判決）

✅ 衝撃・断定型:
   - 「〇〇が100%間違えた」「〇〇完全敗北」
   - 「見たら負け」「〇〇が負けた」
   - デカ文字で衝撃を与える

✅ 数字インパクト型:
   - 「100%の真実」「3秒で判明」
   - 「〇〇%が知らない」
   - 数字で具体性を出す

✅ 疑問喚起型:
   - 「〇〇か、△△か」「どっちが勝つ？」
   - 「え、マジで？」
   - 視聴者を巻き込む

✅ 警告・危機型:
   - 「これヤバい」「知らないと損」
   - 「〇〇終了」
   - 緊急性を出す

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【サブタイトル構造】8-12文字以内

✅ AI判決の結果:
   - 「AIの意外な判決」「AIが選んだのは」
   - 「〇〇を選んだ理由」

✅ 常識の敗北:
   - 「常識が負けた瞬間」「〇〇の敗北」
   - 「思い込みが崩壊」

✅ 衝撃・意外性:
   - 「衝撃の事実」「意外すぎる答え」
   - 「驚きの結論」

✅ 問いかけ:
   - 「あなたはどっち？」「正解はこれ」
   - 「知ってた？」

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【視覚レイアウト提案】必ず含める:

1. **2分割レイアウト** (対立構造):
   - 左: 「常識」「人間」「一般的な考え」
   - 右: 「AI判決」「意外な答え」
   - 境界線: 赤 or 黄色の稲妻

2. **デカ文字レイアウト** (衝撃型):
   - タイトルを画面の70%で表示
   - 強烈な色（赤×白、黄×黒）
   - 影・グローエフェクトで強調

3. **3段レイアウト** (時系列):
   - 上: ニュース見出し
   - 中: タイトル（デカ文字）
   - 下: サブタイトル

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【色スキーム提案】必ず含める:

🔴 危機・警告系:
   - 赤×白、赤×黄、黒×赤
   
⚡ 衝撃・対立系:
   - 黄×黒、オレンジ×白、青×赤
   
💡 発見・驚き系:
   - 青×白、緑×黄、紫×白

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【必須】5パターン生成 - 各パターンは完全に異なるアプローチ:

パターン1: **対立構造型（最推奨）**
   - タイトル: 「〇〇vs△△」形式
   - レイアウト: 2分割
   - 色: 赤×青 or 黄×黒
   
パターン2: **衝撃断定型**
   - タイトル: 「〇〇が100%間違えた」「〇〇完全敗北」
   - レイアウト: デカ文字
   - 色: 赤×白
   
パターン3: **数字インパクト型**
   - タイトル: 数字を強調（「100%」「3秒」など）
   - レイアウト: 数字をデカく
   - 色: 黄×黒
   
パターン4: **疑問喚起型**
   - タイトル: 「〇〇か、△△か」「どっちが勝つ？」
   - レイアウト: 疑問符を大きく
   - 色: 青×白
   
パターン5: **警告・危機型**
   - タイトル: 「これヤバい」「見たら負け」
   - レイアウト: 警告マーク（⚠️）を目立たせる
   - 色: 赤×黄

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【禁止事項】:
❌ タイトルが9文字以上
❌ サブタイトルが13文字以上
❌ 難しい専門用語（「レリバンスエンジニアリング」「ベクトルリンク」など）
❌ 同じようなパターンの繰り返し
❌ 抽象的すぎる表現

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【JSON形式で返答】:
{
  "patterns": [
    {
      "id": 1,
      "name": "対立構造型（最推奨）",
      "title": "常識vsAI",
      "subtitle": "AIが選んだのは",
      "layout": "2分割（左=常識、右=AI）",
      "color_scheme": "赤×青（対立を強調）",
      "text_emphasis": "タイトルをデカく、vsを稲妻で表現",
      "score": 95,
      "reason": "対立構造が明確、一瞬でわかる、常識の敗北を視覚化"
    },
    ...
  ]
}`;

    const userPrompt = `以下の台本から、バズるサムネイル文言を5パターン生成してください。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【台本タイトル】
${scriptTitle}

【Hook】
${scriptHook || 'なし'}

【Body】
${scriptBody || 'なし'}

【CTA】
${scriptCta || 'なし'}

${relatedNewsTitle ? `【関連ニュース】\n${relatedNewsTitle}` : ''}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【台本分析】必ず以下を抽出してください:
1. **対立構造**: Hookから「〇〇 vs △△」を抽出
   - 例: 「駆除 vs 環境」「速さ vs 精度」「伝統 vs 拡散」
   
2. **常識の敗北**: 人間の思い込みとAIの判決のズレ
   - 例: 「人間は〇〇と思うが、AIは△△」
   
3. **時事性・ニュース**: 今日のニュース（あれば）
   - 例: 「ゴキブリ急増」「大相撲ロンドン」「AI新技術」
   
4. **AI判決の意外性**: AIが選んだ意外な答え
   - 例: 「環境を変える」「精度を優先」「広がりを選ぶ」

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【要求事項】絶対厳守:
1. タイトル: **3-8文字以内**（厳守）
2. サブタイトル: **8-12文字以内**（厳守）
3. 5パターン生成（完全に異なるアプローチ）
4. 各パターンに以下を必ず含める:
   - layout: レイアウトの具体的な指示
   - color_scheme: 色の組み合わせ
   - text_emphasis: テキストの強調方法
   - score: 100点満点のスコア
   - reason: このパターンがバズる理由

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【必須パターン構成】:

パターン1: **対立構造型（最推奨）**
   - タイトル: 「〇〇vs△△」形式で対立を明確に
   - 台本のHookから対立構造を抽出
   - 例: 「駆除vs環境」「速さvs精度」「常識vsAI」
   
パターン2: **衝撃断定型**
   - タイトル: 「〇〇が100%間違えた」「〇〇完全敗北」
   - 常識の敗北を強烈に言い切る
   - 例: 「常識が100%負けた」「Google完全敗北」
   
パターン3: **数字インパクト型**
   - タイトル: 数字を強調（台本に数字があれば使用）
   - 例: 「100%の真実」「3秒で判明」「99%が知らない」
   
パターン4: **疑問喚起型**
   - タイトル: 「〇〇か、△△か」「どっちが勝つ？」
   - 視聴者を巻き込む疑問形
   - 例: 「速さか、精度か」「駆除か、環境か」
   
パターン5: **警告・危機型**
   - タイトル: 「これヤバい」「見たら負け」「知らないと損」
   - 緊急性・危機感を煽る
   - 例: 「見たら負け」「知らないとヤバい」

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【重要な注意点】:
✅ タイトルは一瞬で理解できる単純さ
✅ 専門用語は絶対に使わない
✅ 台本の内容に忠実に（創作しない）
✅ 5パターンすべて異なるアプローチで
✅ レイアウト・色・強調方法まで具体的に指示
✅ 縦型動画（9:16）に最適化

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

必ずJSON形式で返答してください。`;

    console.log('🤖 OpenAI API呼び出し中...');

    const response = await openai.chat.completions.create({
      model: 'gpt-4o', // GPT-5はまだサムネ生成には不要、GPT-4oで十分
      messages: [
        { role: 'system', content: systemPrompt },
        { role: 'user', content: userPrompt }
      ],
      max_completion_tokens: 2000,
      response_format: { type: 'json_object' }
    });

    const content = response.choices[0]?.message?.content;
    if (!content) {
      throw new Error('OpenAI APIからの応答が空です');
    }

    const result = JSON.parse(content);
    console.log('✅ サムネイル文言生成完了');
    console.log(`  生成パターン数: ${result.patterns?.length || 0}`);

    return NextResponse.json({
      success: true,
      patterns: result.patterns || []
    });

  } catch (error: any) {
    console.error('❌ サムネイル生成エラー:', error);
    return NextResponse.json(
      { 
        success: false, 
        error: error.message || 'サムネイル生成中にエラーが発生しました' 
      },
      { status: 500 }
    );
  }
}

