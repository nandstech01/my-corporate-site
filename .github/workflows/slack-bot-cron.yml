name: Slack Bot Cron Jobs

on:
  schedule:
    - cron: '0 0 * * *'      # JST 9:00 daily suggestion
    - cron: '0 23,1,3,5,7,9,11,13 * * *'  # JST 8,10,12,14,16,18,20,22 LinkedIn auto-post (2h間隔)
    - cron: '0 1 * * 1'      # JST 10:00 Monday weekly report
    - cron: '0 21,5,13 * * *' # JST 6,14,22 LinkedIn source collector (1日3回)
    - cron: '0 14 * * *'     # JST 23:00 trending collector
    - cron: '0 15 * * *'     # JST 00:00 engagement learner
  workflow_dispatch:
    inputs:
      job:
        description: 'Job to run'
        required: true
        type: choice
        options:
          - daily-suggestion
          - weekly-report
          - engagement-learner
          - linkedin-engagement-learner
          - trending-collector
          - linkedin-source-collector
          - linkedin-auto-post

jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
      - run: npm ci
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: 'services/linkedin-ml/requirements.txt'
      - name: Install & verify Python ML dependencies
        run: |
          pip install -r services/linkedin-ml/requirements.txt
          python3 -c "import xgboost, sklearn, fugashi; print('ML dependencies OK')"
      - run: npx tsx scripts/slack-bot-cron.ts
        env:
          CRON_JOB: ${{ github.event.inputs.job || 'auto' }}
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          SLACK_BOT_TOKEN: ${{ secrets.SLACK_BOT_TOKEN }}
          SLACK_SIGNING_SECRET: ${{ secrets.SLACK_SIGNING_SECRET }}
          SLACK_ALLOWED_USER_IDS: ${{ secrets.SLACK_ALLOWED_USER_IDS }}
          SLACK_DEFAULT_CHANNEL: ${{ secrets.SLACK_DEFAULT_CHANNEL }}
          BRAVE_API_KEY: ${{ secrets.BRAVE_API_KEY }}
          TWITTER_API_KEY: ${{ secrets.TWITTER_API_KEY }}
          TWITTER_API_SECRET: ${{ secrets.TWITTER_API_SECRET }}
          TWITTER_ACCESS_TOKEN: ${{ secrets.TWITTER_ACCESS_TOKEN }}
          TWITTER_ACCESS_TOKEN_SECRET: ${{ secrets.TWITTER_ACCESS_TOKEN_SECRET }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          REDDIT_CLIENT_ID: ${{ secrets.REDDIT_CLIENT_ID }}
          REDDIT_CLIENT_SECRET: ${{ secrets.REDDIT_CLIENT_SECRET }}
          LINKEDIN_ACCESS_TOKEN: ${{ secrets.LINKEDIN_ACCESS_TOKEN }}
          LINKEDIN_PERSON_ID: ${{ secrets.LINKEDIN_PERSON_ID }}
          LINKEDIN_TOKEN_EXPIRES_AT: ${{ secrets.LINKEDIN_TOKEN_EXPIRES_AT }}

